{
  "name": "signal-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun dist/src/index.js",
    "build": "tsc -p tsconfig.build.json && bun scripts/copy-migrations.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "bun test ./tests",
    "check-architecture": "bun scripts/check-architecture.ts",
    "check-language": "bun scripts/check-language.ts",
    "check-audit": "bun audit --audit-level=high",
    "check-dead-code": "knip-bun --config knip.json --include files,dependencies,unlisted,binaries",
    "knip": "knip-bun --config knip.json",
    "rehearse-migration": "bun scripts/rehearse-migration.ts",
    "install-hooks": "git config core.hooksPath .githooks",
    "check-migrations": "bun scripts/check-migrations.ts",
    "check": "bun run lint && bun run typecheck && bun run test && bun run check-migrations && bun run check-architecture && bun run check-language && bun run check-dead-code && bun run check-audit && bun run build",
    "poll": "bun src/cli.ts poll",
    "status": "bun src/cli.ts status",
    "events": "bun src/cli.ts events",
    "suppressions": "bun src/cli.ts suppressions"
  },
  "dependencies": {
    "hono": "^4.12.31",
    "zod": "^4.4.3",
    "fast-xml-parser": "^5.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/bun": "^1.3.14",
    "knip": "^6.35.1",
    "typescript": "^7.0.2"
  }
}
